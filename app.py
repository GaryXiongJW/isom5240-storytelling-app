"""ISOM5240 submit entry — landscape kid UI (optimized)."""

# Canvas / Streamlit Cloud Main file stays app.py.
# Implementation lives in app_uiv2.py so we can iterate UI without losing the entrypoint.
from app_uiv2 import main

if __name__ == "__main__":
    main()
